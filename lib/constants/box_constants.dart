import 'package:animo/models/base_data.dart';

class BoxConstants {
  static const main = 'animo';

  static const tokenKey = 'token';
  static const userKey = 'user';

  static String library(MediaType type) => '${type.name}.library';
}
